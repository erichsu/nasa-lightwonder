# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LightWonder' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for UI
  pod 'SwiftMessages'
  pod 'PKHUD'
  pod 'SwiftIconFont'
  pod 'IQKeyboardManagerSwift'
  pod 'Charts'
  pod 'SideMenu'
  pod 'Floaty'
  pod 'Hero'
  
  # Pods for Common utils
  pod 'Alamofire'
  pod 'JASON'
  pod 'XCGLogger'
  pod 'Kingfisher'

  target 'LightWonderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LightWonderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
